interface:
  display_name: "Draw.io.ai"
  short_description: "Install and use drawio MCP fast."
  default_prompt: "Use $draw-io-ai to install, configure, verify, and quickly start the Next AI Draw.io MCP server with a visible browser preview."

dependencies:
  tools:
    - type: "mcp"
      value: "drawio"
      description: "Next AI Draw.io MCP server"

policy:
  allow_implicit_invocation: true
